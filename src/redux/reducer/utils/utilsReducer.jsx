import { createSlice } from "@reduxjs/toolkit";
import { uploadImage } from "./utilsApi";

const utilsSlice= createSlice({
    initialState:{
        loading:false,
        error:null,
        imageUpload:null,
    },
    name:"utils",
    extraReducers:(builders)=>{
        bui
    }
})